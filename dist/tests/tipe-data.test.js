"use strict";
describe("Data type", () => {
    it("Should must delcare", () => {
        let name = "Yahya Ibrahim";
        const balance = 100000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // Error
        // name = 1000;
    });
});
