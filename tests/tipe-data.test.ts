describe("Data type", () => {
  it("Should must delcare", () => {
    let name: string = "Yahya Ibrahim";
    const balance: number = 100000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);

    // Error
    // name = 1000;
  });
});
